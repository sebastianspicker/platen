import { basename } from 'node:path';
export async function runBatesNumberingCommand(application, command, document, stdout, signal, runtime) { runtime.cancelled(signal); const result = await application.batesNumbering.add(document.id, { profile: 'local-pdf-bates-numbering-v1', sourceSha256: document.sha256, pages: command.pages, start: command.start, prefix: command.prefix, suffix: command.suffix, padding: command.padding, position: command.position, margin: command.margin, fontSize: command.fontSize }, { signal }); runtime.cancelled(signal); const artifact = application.store.getArtifact(result.artifact.id); await runtime.copyExclusive(artifact.filePath, command.output, signal); await runtime.emit(stdout, { ...result, artifact: { ...result.artifact, output: basename(command.output) }, localOnly: true, sourceBound: true }); }
