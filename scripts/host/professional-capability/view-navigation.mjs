import {
  documentOpenLocal,
  documentOpenDragDrop,
  documentClose,
  documentDownloadOriginal,
  viewerNativeRender,
  viewerZoomPreview,
  viewerRotatePreview,
  viewerFullscreen,
  viewerThumbnails,
  viewerSearch,
  viewerAdvancedSearch,
  viewerPageLayouts,
  viewerMultidocumentTabs,
  viewerSplitView,
  viewerReflow,
  viewerReadAloud,
  viewerAttachments,
  viewerLayers,
  viewerDestinations,
  viewerPageLabels,
  viewerDocumentProperties,
  viewerSelectCopy,
  viewerLoupe,
  viewerSnapshot,
  viewerRulersGrid,
  viewerPresentationMode,
  viewerNavigationHistory,
} from './view-navigation-ops.mjs';
import { viewerBookmarks } from './view-navigation-bookmarks.mjs';

export const handlers = Object.freeze({
  async 'document.open.local'(ctx = {}) { return documentOpenLocal(ctx); },
  async 'document.open.drag-drop'(ctx = {}) { return documentOpenDragDrop(ctx); },
  async 'document.close'(ctx = {}) { return documentClose(ctx); },
  async 'document.download.original'(ctx = {}) { return documentDownloadOriginal(ctx); },
  async 'viewer.native.render'(ctx = {}) { return viewerNativeRender(ctx); },
  async 'viewer.zoom.preview'(ctx = {}) { return viewerZoomPreview(ctx); },
  async 'viewer.rotate.preview'(ctx = {}) { return viewerRotatePreview(ctx); },
  async 'viewer.fullscreen'(ctx = {}) { return viewerFullscreen(ctx); },
  async 'viewer.thumbnails'(ctx = {}) { return viewerThumbnails(ctx); },
  async 'viewer.search'(ctx = {}) { return viewerSearch(ctx); },
  async 'viewer.advanced-search'(ctx = {}) { return viewerAdvancedSearch(ctx); },
  async 'viewer.bookmarks'(ctx = {}) { return viewerBookmarks(ctx); },
  async 'viewer.page-layouts'(ctx = {}) { return viewerPageLayouts(ctx); },
  async 'viewer.multidocument-tabs'(ctx = {}) { return viewerMultidocumentTabs(ctx); },
  async 'viewer.split-view'(ctx = {}) { return viewerSplitView(ctx); },
  async 'viewer.reflow'(ctx = {}) { return viewerReflow(ctx); },
  async 'viewer.read-aloud'(ctx = {}) { return viewerReadAloud(ctx); },
  async 'viewer.attachments'(ctx = {}) { return viewerAttachments(ctx); },
  async 'viewer.layers'(ctx = {}) { return viewerLayers(ctx); },
  async 'viewer.destinations'(ctx = {}) { return viewerDestinations(ctx); },
  async 'viewer.page-labels'(ctx = {}) { return viewerPageLabels(ctx); },
  async 'viewer.document-properties'(ctx = {}) { return viewerDocumentProperties(ctx); },
  async 'viewer.select-copy'(ctx = {}) { return viewerSelectCopy(ctx); },
  async 'viewer.loupe'(ctx = {}) { return viewerLoupe(ctx); },
  async 'viewer.snapshot'(ctx = {}) { return viewerSnapshot(ctx); },
  async 'viewer.rulers-grid'(ctx = {}) { return viewerRulersGrid(ctx); },
  async 'viewer.presentation-mode'(ctx = {}) { return viewerPresentationMode(ctx); },
  async 'viewer.navigation-history'(ctx = {}) { return viewerNavigationHistory(ctx); },
});

